description: Forbidden. The client has provided a token that does not authorize them to perform the action implemented by the API endpoint.
headers:
  RequestID:
    $ref: ../headers/RequestId.yaml
content:
  application/json:
    schema:
      $ref: ./helpers/Error403.yaml
    example:
      errorMessage: Insufficient authorization to perform request.