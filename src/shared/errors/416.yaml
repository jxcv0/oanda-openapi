description: Range Not Satisfiable. The client has specified a range that is invalid or cannot be processed.
headers:
  RequestID:
    $ref: ../headers/RequestId.yaml
content:
  application/json:
    schema:
      $ref: ./helpers/Error416.yaml