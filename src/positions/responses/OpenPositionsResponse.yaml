type: object
properties:
  positions:
    description: The list of open Positions in the Account.
    type: array
    items:
      $ref: ../types/Position.yaml
  lastTransactionID:
    description: The ID of the most recent Transaction created for the Account
    type: integer