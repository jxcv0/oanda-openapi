description: A TakeProfitOrderTransaction represents the creation of a TakeProfit Order in the user's Account.
type: object
allOf:
  - $ref: ../../shared/Transaction.yaml
  - $ref: ./TakeProfitOrderTransactionBase.yaml
