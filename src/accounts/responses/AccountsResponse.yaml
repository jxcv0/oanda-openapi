type: object
properties:
  accounts:
    description: The list of Accounts the client is authorized to access and their associated properties.
    type: array
    items:
      $ref: ../types/AccountProperties.yaml